import os
import pandas as pd
from datetime import datetime
import re

# ==================== CONFIGURACIÓN ====================
RUTA_BASE_ACLARABOT = "../Carpeta/Aclarabot"
RUTA_DESTINO = "c:/Copia-reporte-Aclarabot"
RUTA_ACLARAPP = "c:/Copia-reporte-Aclarabot/Copy-Aclarapp/TablaCopiadaDeAclarapp.csv"
ENTORNOS = ["DEV", "UAT", "PROD"]

# ==================== FUNCIONES PARA ACLARABOT ====================
def procesar_aclarabot():
    """Procesa los archivos Log.csv de Aclarabot"""
    print("Procesando archivos Aclarabot...")
    
    # Preparar fecha
    ahora = datetime.now()
    fecha_archivo = f"{ahora.strftime('%d-%b')}.csv"
    ruta_final = os.path.join(RUTA_DESTINO, fecha_archivo)
    
    # Crear carpeta si no existe
    if not os.path.exists(RUTA_DESTINO):
        os.makedirs(RUTA_DESTINO)
    
    # Lista para guardar datos
    todos_datos = []
    
    for entorno in ENTORNOS:
        # Construir ruta del archivo
        ruta_archivo = os.path.join(
            RUTA_BASE_ACLARABOT, 
            entorno, 
            "Log", 
            str(ahora.year), 
            ahora.strftime("%m"), 
            "Log.csv"
        )
        
        if os.path.exists(ruta_archivo):
            print(f"  Leyendo {entorno}...")
            try:
                # Leer solo primeras 12 columnas
                df = pd.read_csv(ruta_archivo, usecols=range(12), 
                               encoding='utf-8', on_bad_lines='skip')
                
                # Agregar columna de entorno
                df.insert(0, 'ENTORNO', entorno)
                
                todos_datos.append(df)
                print(f"    ✓ {len(df)} filas")
                
            except Exception as e:
                print(f"    ✗ Error en {entorno}: {e}")
        else:
            print(f"  ✗ Archivo no encontrado: {ruta_archivo}")
    
    # Combinar y guardar si hay datos
    if todos_datos:
        df_final = pd.concat(todos_datos, ignore_index=True)
        df_final.to_csv(ruta_final, index=False, encoding='utf-8-sig')
        print(f"\n✓ Aclarabot guardado: {ruta_final}")
        print(f"  Total: {len(df_final)} filas")
        return ruta_final
    else:
        print("\n✗ No se procesaron archivos de Aclarabot")
        return None

# ==================== FUNCIONES PARA ACLARAPP ====================
def detectar_separador(linea):
    """Detecta el tipo de separador en una línea"""
    # Prioridad de separadores
    if '|' in linea:
        return '|'
    elif '\t' in linea:
        return '\t'
    else:
        # Contar espacios consecutivos
        espacios_consecutivos = re.findall(r'\s{2,}', linea)
        if espacios_consecutivos:
            return 'multiples_espacios'
        return 'simple_espacio'

def procesar_linea_con_separador(linea, separador):
    """Procesa una línea según el separador detectado"""
    if separador == '|':
        # Separar por pipe, eliminar espacios al inicio y fin de cada campo
        partes = [part.strip() for part in linea.split('|') if part.strip() != '']
        return partes
    elif separador == '\t':
        # Separar por tab
        return linea.split('\t')
    elif separador == 'multiples_espacios':
        # Separar por 2 o más espacios
        return re.split(r'\s{2,}', linea.strip())
    else:
        # Separar por cualquier espacio (último recurso)
        return linea.split()

def extraer_tabla_aclarapp(texto):
    """Extrae la tabla del texto copiado de la web"""
    
    # Buscar donde empieza la tabla
    lineas = [linea.rstrip('\r') for linea in texto.split('\n')]
    
    # Patrones para buscar el inicio de la tabla
    patrones_inicio = [
        r'ID Aclaración[\s\|]Registro[\s\|]Folio[\s\|]CIS',
        r'ID Aclaración.*Registro.*Folio.*CIS',
        'ID Aclaración|Registro|Folio|CIS',
        'ID Aclaración\tRegistro\tFolio\tCIS',
        'ID Aclaración  Registro  Folio  CIS',
        'ID Aclaración'
    ]
    
    inicio_idx = -1
    separador_detectado = None
    
    # Buscar inicio de tabla
    for i, linea in enumerate(lineas):
        linea_limpia = linea.strip()
        
        # Verificar si esta línea contiene los encabezados
        for patron in patrones_inicio:
            if patron in linea_limpia:
                inicio_idx = i
                separador_detectado = detectar_separador(linea)
                print(f"  Tabla encontrada en línea {i+1}")
                print(f"  Separador detectado: {repr(separador_detectado)}")
                break
        
        if inicio_idx != -1:
            break
    
    if inicio_idx == -1:
        print("  No se encontró el inicio de la tabla")
        print("  Buscando líneas que contengan 'ID Aclaración'...")
        for i, linea in enumerate(lineas[:20]):
            if 'ID Aclaración' in linea:
                print(f"  Línea {i+1}: {linea[:100]}")
        return None
    
    # Obtener encabezados usando el separador detectado
    encabezados = procesar_linea_con_separador(lineas[inicio_idx], separador_detectado)
    
    # Si no tenemos suficientes encabezados, usar los predeterminados
    if len(encabezados) < 11:
        print(f"  Sólo se encontraron {len(encabezados)} encabezados, usando predeterminados")
        encabezados = [
            'ID Aclaración', 'Registro', 'Folio', 'CIS', 'Fecha Creación',
            'Fecha Enviado', 'Fecha Asignacion', 'Fecha Atendido', 
            'Estado Actual', 'Intentos', 'Acciones'
        ]
    else:
        print(f"  {len(encabezados)} encabezados detectados")
    
    # Recopilar filas de datos
    datos = []
    
    for i in range(inicio_idx + 1, len(lineas)):
        linea = lineas[i].strip()
        
        # Detener si encontramos el final
        if 'Mostrando' in linea or 'Mostrar' in linea or not linea:
            print(f"  Fin de tabla detectado en línea {i+1}")
            break
        
        # Verificar si es una línea de datos (debe tener varios campos)
        if len(linea) < 20:  # Línea muy corta probablemente no sea datos
            continue
        
        # Procesar la línea con el separador detectado
        fila = procesar_linea_con_separador(lineas[i], separador_detectado)
        
        # Asegurar que tenga al menos 10 columnas (Acciones puede estar vacía)
        if len(fila) >= 10:
            # Si tiene exactamente 10, agregar campo vacío para Acciones
            if len(fila) == 10:
                fila.append('')
            datos.append(fila[:11])
        elif len(fila) >= 8:  # Intentar con menos columnas
            print(f"  Advertencia: Línea {i+1} tiene sólo {len(fila)} columnas: {fila[:3]}...")
            # Rellenar con valores vacíos
            while len(fila) < 11:
                fila.append('')
            datos.append(fila[:11])
    
    if datos:
        print(f"  {len(datos)} filas extraídas")
        
        # Crear DataFrame
        try:
            df = pd.DataFrame(datos, columns=encabezados[:11])
            print(f"  DataFrame creado: {len(df)} filas x {len(df.columns)} columnas")
            return df
        except Exception as e:
            print(f"  Error creando DataFrame: {e}")
            print(f"  Número de encabezados: {len(encabezados)}")
            print(f"  Número de columnas en datos: {len(datos[0]) if datos else 0}")
            
            # Intentar con columnas genéricas si hay error
            if datos:
                columnas_genericas = [f'Columna_{i+1}' for i in range(len(datos[0]))]
                df = pd.DataFrame(datos, columns=columnas_genericas)
                print(f"  DataFrame creado con columnas genéricas")
                return df
            return None
    else:
        print("  No se extrajeron datos")
        return None

def procesar_aclarapp():
    """Procesa el archivo de Aclarapp copiado de la web"""
    print("\nProcesando archivo Aclarapp...")
    
    # Crear carpeta si no existe
    carpeta_aclarapp = os.path.dirname(RUTA_ACLARAPP)
    if not os.path.exists(carpeta_aclarapp):
        os.makedirs(carpeta_aclarapp)
    
    if os.path.exists(RUTA_ACLARAPP):
        print(f"  Archivo encontrado: {RUTA_ACLARAPP}")
        
        try:
            # Leer archivo como texto
            with open(RUTA_ACLARAPP, 'r', encoding='utf-8') as f:
                contenido = f.read()
            
            print(f"  Tamaño del archivo: {len(contenido)} caracteres")
            
            # Extraer tabla
            df = extraer_tabla_aclarapp(contenido)
            
            if df is not None and not df.empty:
                # Guardar tabla limpia
                ruta_limpia = RUTA_ACLARAPP.replace('.csv', '_LIMPIADO.csv')
                df.to_csv(ruta_limpia, index=False, encoding='utf-8-sig')
                
                print(f"  ✓ Tabla guardada: {ruta_limpia}")
                print(f"    {len(df)} filas, {len(df.columns)} columnas")
                
                # Mostrar vista previa
                print("\n  Vista previa:")
                print(df.head(3).to_string())
                
                return df
            else:
                print("  ✗ No se pudo extraer la tabla")
                print("  Mostrando primeras 10 líneas del archivo:")
                lineas = contenido.split('\n')
                for i, linea in enumerate(lineas[:10]):
                    print(f"  {i+1:3}: {linea[:100]}")
                return None
                
        except Exception as e:
            print(f"  ✗ Error procesando Aclarapp: {e}")
            return None
    else:
        print(f"  ✗ Archivo no encontrado: {RUTA_ACLARAPP}")
        print("  💡 Copia el texto de la web en este archivo")
        return None

# ==================== FUNCIÓN PRINCIPAL ====================
def main():
    """Ejecuta todo el proceso"""
    print("=" * 60)
    print("SISTEMA DE REPORTES - ACLARABOT Y ACLARAPP")
    print("=" * 60)
    
    # Procesar Aclarabot
    archivo_aclarabot = procesar_aclarabot()
    
    # Procesar Aclarapp
    df_aclarapp = procesar_aclarapp()
    
    print("\n" + "=" * 60)
    print("RESUMEN DEL PROCESO")
    print("=" * 60)
    
    if archivo_aclarabot:
        print(f"✓ Aclarabot: {archivo_aclarabot}")
    
    if df_aclarapp is not None:
        print(f"✓ Aclarapp: {len(df_aclarapp)} filas procesadas")
        print(f"  Archivo limpio guardado como: TablaCopiadaDeAclarapp_LIMPIADO.csv")
    
    print("\n✅ Proceso completado")

# ==================== EJECUCIÓN ====================
if __name__ == "__main__":
    main()
