from pywinauto import Desktop

print("Listando ventanas detectadas por backend=win32...\n")

windows = Desktop(backend="win32").windows()

for w in windows:
    try:
        title = w.window_text()
        if title.strip() != "":
            print("→", title)
    except:
        pass
