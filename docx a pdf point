import re
from pathlib import Path
import shutil
import os

# Configuración
CARPETA_HTML = Path(r"C:\Users\Erick Baez\Documents\html a pdf\ejemplos")
CARPETA_SALIDA = Path(r"C:\Users\Erick Baez\Documents\html a pdf\ejemplos\salida")
URL_SHAREPOINT = "https://tubanco.sharepoint.com/sites/misitio/Documentos%20compartidos/Carpeta/"

# Crear carpeta de salida
CARPETA_SALIDA.mkdir(parents=True, exist_ok=True)

def procesar_html(html_path):
    """Procesa el HTML manteniendo rutas relativas de imágenes"""
    with open(html_path, 'r', encoding='utf-8', errors='ignore') as f:
        contenido = f.read()
    
    # 1. Actualizar solo enlaces a .htm/.html principales (no en _archivos)
    contenido = re.sub(
        r'href=["\']([^"\']*\.htm[l]?)["\']',
        lambda m: f'href="{URL_SHAREPOINT}{os.path.splitext(m.group(1))[0]}.pdf"',
        contenido,
        flags=re.IGNORECASE
    )
    
    # 2. Mantener rutas originales de imágenes en _archivos
    contenido = re.sub(
        r'src=["\']([^"\']*_archivos[\\/][^"\']*\.(?:jpe?g|png|gif|bmp|svg|webp|ico))["\']',
        lambda m: f'src="{m.group(1).replace("\\", "/")}"',  # Normalizar a /
        contenido,
        flags=re.IGNORECASE
    )
    
    return contenido

def copiar_estructura_completa(html_path, output_dir):
    """Copia el archivo HTML y toda su estructura de _archivos"""
    # Copiar archivo principal
    shutil.copy2(html_path, output_dir)
    
    # Copiar carpeta _archivos manteniendo estructura
    html_name = html_path.stem
    carpeta_archivos = html_path.parent / f"{html_name}_archivos"
    
    if carpeta_archivos.exists():
        dest_dir = output_dir / f"{html_name}_archivos"
        shutil.copytree(carpeta_archivos, dest_dir, dirs_exist_ok=True)

# Proceso principal
print("Iniciando procesamiento manteniendo estructura de imágenes...")

for html_file in CARPETA_HTML.glob("*.htm*"):
    if html_file.is_file() and not '_archivos' in str(html_file.parent):
        print(f"\n● Procesando: {html_file.name}")
        
        try:
            # Procesar HTML
            contenido = procesar_html(html_file)
            
            # Guardar archivo modificado
            output_html = CARPETA_SALIDA / html_file.name
            output_html.write_text(contenido, encoding='utf-8')
            
            # Copiar estructura completa
            copiar_estructura_completa(html_file, CARPETA_SALIDA)
            
            print(f"  ✅ Archivo procesado: {html_file.name}")
            print(f"  📁 Carpeta copiada: {html_file.stem}_archivos/")
        except Exception as e:
            print(f"  ❌ Error procesando {html_file.name}: {str(e)}")

print("\nProceso completado. Estructura completa copiada a:", CARPETA_SALIDA)
print("Las imágenes deberían mostrarse correctamente manteniendo las rutas relativas.")
